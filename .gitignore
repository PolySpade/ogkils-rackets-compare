# build output
dist/
.astro/

# dependencies
node_modules/
.pnp
.pnp.*

# generated types
*.tsbuildinfo

# environment
.env
.env.*

# macOS
.DS_Store

# logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea/

# unrelated tooling output (kept out of the build)
graphify-out/

# deploy
.vercel
.netlify

# Reference material and tool output — not build inputs, and the brochure alone is 29MB,
# which every clone and every Vercel build would have to pull down. DESIGN.md links to
# it as a local reference; keep your own copy outside git (or in cloud storage).
OGKILS-2026 Brochure.pdf
.impeccable/

# stale working screenshots
compare-mobile.png

# Brand masters dropped at the repo root. Leading slash matters: a bare "assets/" would
# also match src/assets/, which holds the racket artwork the build imports.
/assets/
